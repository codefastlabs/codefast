{
  "$schema": "./node_modules/turbo/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "check-types": {
      "dependsOn": ["build", "^check-types"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["build", "^lint"]
    },
    "preview": {
      "dependsOn": ["build", "^build"],
      "cache": false
    },
    "start": {},
    "test": {
      "dependsOn": ["build", "^test"]
    },
    "test:coverage": {
      "dependsOn": ["build", "^test:coverage"],
      "outputs": ["coverage/**"]
    },
    "test:coverage:ci": {
      "dependsOn": ["build", "^test:coverage:ci"],
      "outputs": ["coverage/**"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    }
  }
}
