{
  "$schema": "./node_modules/turbo/schema.json",
  "cacheMaxAge": "7d",
  "globalEnv": ["SKIP_INSTALL_SIMPLE_GIT_HOOKS"],
  "tasks": {
    "//#format": {
      "cache": false
    },
    "//#format:check": {},
    "//#lint": {},
    "//#knip": {},
    "//#lint:fix": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "env": ["VITE_GA4_MEASUREMENT_ID"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".output/**", ".vercel/output/**"],
      "outputLogs": "new-only"
    },
    "check-types": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "tsconfig.*.json"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "preview": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"]
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"],
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"]
    },
    "test:integration": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"]
    },
    "test:type": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"]
    },
    "test:unit": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.*", "vitest.setup.*"]
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "bench": {
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "tsconfig.*.json"],
      "passThroughEnv": ["BENCH_ISOLATE", "BENCH_MODE", "BENCH_ONLY", "BENCH_TRIALS", "BENCH_VERBOSE"]
    },
    "bench:isolate": {
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "tsconfig.*.json"],
      "passThroughEnv": ["BENCH_ISOLATE", "BENCH_MODE", "BENCH_ONLY", "BENCH_TRIALS", "BENCH_VERBOSE"]
    },
    "bench:serve": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["BENCH_PORT"]
    }
  }
}
