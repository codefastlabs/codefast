---
"@codefast/di-testing": patch
---

`README.md` no longer quotes the `@codefast/di` peer range, which had fallen behind `package.json` — the release tooling
moves that range, so the manifest is the one place it is stated.
