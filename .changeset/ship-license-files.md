---
"@codefast/di": patch
"@codefast/di-testing": patch
"@codefast/tailwind-variants": patch
"@codefast/theme": patch
"@codefast/tracking": patch
"@codefast/ui": patch
---

Ship the MIT `LICENSE` file in the published package. `files` already listed it, but the file was missing from the
package directory, so the tarball had none.
