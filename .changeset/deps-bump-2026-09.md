---
"@codefast/cli": patch
"@codefast/theme": patch
"@codefast/ui": patch
---

Raise the published dependency floors: `zod` to 4.5.4 (`@codefast/cli`, `@codefast/theme`), `lucide-react` to 1.41.0 and
`react-hook-form` to 7.87.0 (`@codefast/ui`).
