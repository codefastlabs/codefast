---
"@benchmark/di": patch
---

The A/B protocol in `BENCH_GUIDE.md` names `BENCH_LIBRARY=@codefast/di`: a paired A/B compares the subject with itself,
so the rivals' subprocesses are wall clock spent on nothing.
