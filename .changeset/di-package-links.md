---
"@codefast/di": patch
"@codefast/di-testing": patch
---

`package.json` now carries `homepage` and `bugs`, so npm links the package README and the issue tracker the way the
other `@codefast/*` packages already do.
