{
  "name": "codefast",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "bench": "turbo run bench --concurrency=1",
    "bench:di": "turbo run bench --filter=@codefast/benchmark-di-inversify",
    "bench:serve": "turbo run bench:serve",
    "build:packages": "turbo run build --filter=@codefast*/*...",
    "check": "turbo run lint format:check check-types",
    "check-types": "turbo run check-types",
    "check:fix": "turbo run lint:fix && turbo run format",
    "clean": "turbo clean",
    "clean:cache": "pnpm store prune",
    "clean:modules": "pnpm -r exec rm -rf node_modules && rm -rf node_modules pnpm-lock.yaml",
    "codefast": "node ./packages/cli/dist/bin.mjs",
    "cli:arrange-analyze": "pnpm run codefast arrange analyze",
    "cli:arrange-apply": "pnpm run codefast arrange apply",
    "cli:arrange-preview": "pnpm run codefast arrange preview",
    "cli:mirror-sync": "pnpm run codefast mirror sync",
    "deps:install": "pnpm install && pnpm build:packages",
    "deps:outdated": "pnpm outdated --recursive",
    "deps:reinstall": "pnpm clean && pnpm clean:modules && pnpm install && pnpm build:packages",
    "deps:update": "pnpm dlx taze@latest latest --recursive --interactive --write --install && pnpm build:packages",
    "dev": "turbo run dev",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "knip": "knip",
    "lint": "oxlint --deny-warnings",
    "lint:fix": "oxlint --fix --deny-warnings",
    "prepare": "simple-git-hooks",
    "release": "changeset",
    "release:canary:enter": "changeset pre enter canary",
    "release:canary:exit": "changeset pre exit",
    "version-packages": "changeset version && pnpm run codefast tag",
    "start": "turbo run start",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:e2e": "turbo run test:e2e",
    "test:integration": "turbo run test:integration",
    "test:type": "turbo run test:type",
    "test:unit": "turbo run test:unit",
    "test:watch": "turbo run test:watch",
    "verify": "pnpm build:packages && turbo run lint:fix && turbo run format && turbo run check-types test:coverage"
  },
  "devDependencies": {
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "knip": "catalog:",
    "lint-staged": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "simple-git-hooks": "catalog:",
    "turbo": "catalog:"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@11.0.9"
}
