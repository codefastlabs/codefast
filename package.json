{
  "name": "codefast",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "turbo run bench",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=@codefast*/*...",
    "check": "turbo run lint format:check check-types",
    "check-types": "turbo run check-types",
    "check:fix": "turbo run lint:fix && turbo run format",
    "clean": "turbo clean",
    "clean:cache": "pnpm store prune",
    "clean:modules": "pnpm -r exec rm -rf node_modules && rm -rf node_modules pnpm-lock.yaml",
    "cli:mirror-sync": "pnpm exec codefast mirror sync",
    "cli:arrange-analyze": "pnpm exec codefast arrange analyze",
    "cli:arrange-apply": "pnpm exec codefast arrange apply",
    "cli:arrange-preview": "pnpm exec codefast arrange preview",
    "deps:install": "pnpm install && pnpm build:packages",
    "deps:outdated": "pnpm outdated",
    "deps:reinstall": "pnpm clean && pnpm install && pnpm build:packages",
    "deps:update": "pnpm up --latest --recursive",
    "deps:update:interactive": "pnpm up --latest --recursive --interactive",
    "dev": "pnpm build:packages && turbo run dev",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint --deny-warnings",
    "lint:fix": "oxlint --fix --deny-warnings",
    "prepare": "simple-git-hooks",
    "release": "changeset",
    "release:canary:enter": "changeset pre enter canary",
    "release:canary:exit": "changeset pre exit",
    "start": "turbo run start",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:coverage:ci": "turbo run test:coverage:ci",
    "test:watch": "turbo run test:watch"
  },
  "devDependencies": {
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@codefast/cli": "workspace:*",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@commitlint/types": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "lint-staged": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "simple-git-hooks": "catalog:",
    "ts-node": "catalog:",
    "tsx": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "@tailwindcss/oxide",
      "core-js",
      "esbuild",
      "sharp",
      "simple-git-hooks",
      "unrs-resolver"
    ]
  }
}
