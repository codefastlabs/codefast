/**
 * The manifest file name that marks a package directory.
 *
 * @since 0.10.0
 */
export const packageJsonFileName = "package.json";

/**
 * The pnpm workspace manifest file name that marks a workspace root.
 *
 * @since 0.10.0
 */
export const workspaceYamlFileName = "pnpm-workspace.yaml";

/**
 * The build output directory name packages emit to.
 *
 * @since 0.10.0
 */
export const distDirName = "dist";
