/** Sticky header height in Tailwind units (h-12 = 3rem). Used for scroll-margin offsets. */
export const LAYOUT_HEADER_HEIGHT = "3rem";

/** Sticky offset below the site header (h-12). */
export const STICKY_OFFSET_HEADER = "top-12";

/** Sticky offset below header with breathing room — sidebar nav. */
export const STICKY_OFFSET_BELOW_HEADER = "top-20";

/** scroll-mt for gallery letter bands and cards (header + mobile jump nav). */
export const SCROLL_MT_GALLERY = "scroll-mt-28";

/** Sticky offset for detail page TOC sidebar. */
export const STICKY_OFFSET_DETAIL_TOC = "top-24";

/** scroll-mt for in-page doc anchors (header only). */
export const SCROLL_MT_ANCHOR = "scroll-mt-24";
