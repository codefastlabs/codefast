import type { ComponentMetaInput } from "#/registry/components";

export const meta: ComponentMetaInput = {
  name: "Message",
  category: "display",
  description: "Chat message row pairing an avatar with a content column; mirrors for the current author.",
};
