{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@codefast/typescript-config/library.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "comment": "Editor-default only. Only includes library-agnostic files so decorator-mode conflicts never surface here. check-types uses the three explicit tsconfigs (codefast / inversify / runner).",
  "include": ["src/harness/**/*.ts", "src/scenarios/types.ts", "src/fixtures/realistic-graph.ts"]
}
